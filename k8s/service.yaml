apiVersion: v1
kind: Service
metadata:
  name: mediasoup-demo-svc
  namespace: mediasoup
spec:
  type: LoadBalancer
  selector:
    app: mediasoup-demo
  ports:
    - name: signaling
      port: 4443
      targetPort: 4443
      protocol: TCP

